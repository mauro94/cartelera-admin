import React from 'react'

export const TagIcon = ({ children }) => (
    <div className='tag icon'>{children}</div>
)

export const Tag = ({ children }) => (
    <div className='tag'>{children}</div>
)